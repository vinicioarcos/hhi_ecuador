# Data inventory

La replica principal usa el inventario compilado por `src/build_publication_tables.py` en:

- `outputs/tables/publication_data_inventory.csv`

Este proyecto no mantiene una segunda base paralela. La capa `02_DATOS/` documenta el flujo ya existente en `data/`.
